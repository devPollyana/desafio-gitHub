def gerar_matriz (n_linhas, n_colunas):
        ''' 
          (int,int) -> matrix
        '''
        matriz = []
        matriz.append(" ")
        matriz.append([" "]*num_linhas)

        i = 0
        while i < len(matriz):
            matriz[i].append(" ")
            i += 1


        return matriz